    object-orented Programming
    object contain data in the from of(객체는 두가지를 포함한다.)
    Fields = attributes,properties. 라고 불림
    code,as methods = 동작,또는 기능.(=procedures:절차라고 부름).
    oop=easier(용이하다.)test,maintain,refine,and extend.
    ****
    oop=computer programing을 write(작성) 할때 creating in your computer= a model of some part of the world. 즉 현실세계의 일부를 컴퓨터 내부에 모델링 하는것이다.
    object are created from **clsses** = 객체는 클래스로부터 생성.
    classes = object의 type/category를  설명한다.
    object = class의 구체적인 instance를 나타냅니다.
    ****
    classes = blueprints.
    마치 사양이 다다른 설계도를 가진 아이폰 처럼.
    ****
    object have operations that can be invoked
    객체는 동작(메서드)를 가지고 있다. 자바로 치면 methods
    object객체는 state를 가지고 잇습니다. 마치 snapshot처럼 특정 상태의 모습. state:stored in the fields.
    예시 stunden classes
    studentsNumber = 이건 변하지 않는다. 고정된 식별번호.
    bookBorrowed= 대출한 책수.
    ****
    포켓몬 classes예시.
    피카츄,이상해씨,파이리:instance.
    각각의 포켓몬은 공통적인 attribute와 method를 가진다.
    ****
    classes:  a class represnts a general kind/category:일반적인 분류 또는 타임.
    object: individual objects are created/instantiated from the class 클래스에서 생성된특정 개체. 
    instance = object.
    ****
    circle object has type = int,string,boolean.
    int diameter = 68 가능 그러나 "blue"는 불가능.
    ****
    Many instances can be created from a single class = 하나의 클래스로부터 여러개의 인스턴스를 만들수 있다.
    class defines what fields = 클래스는 어떤 필드(속성)이 있는지 정의한다.
    each object stores its own set of values(state):각 객체는 자기만의 필드값(상태)를 의미한다.
    ****
    Methods correspond to things we might "ask" an object to do:메서드는 객체에게 무엇을 하라고 명령하는동작.
    Methods many have Parameters. information needed to perform a task= 매개변수(파라미터)는 매서드가 일을 수행하기 위해 필요한 추가정보.
    Circle.moveRight();
    Circle.ChangeColor("red);
    매서드이름이 곧 동작. 괄호안 값이 파라미터(정보입력).
    ****
    name of method + parameters = signature.
    메서드를 호출하기 위해서는 메서드 시그니처를 정확히 알하야한다.
    쉐프 웨이터 그림.
    각역할은 객체이며
    각동작은 메서드이다 
    wairer:bringmenu() takeorder() bringmeal()
    ****
    object 객체:real world의 개념을 컴퓨터 program에 구현한 실체.
    예시)피카츄,circle.
    classs:object를 만들기 위한 blueprint.
    method:객체가 수행할수 있는 동작. attack(),changeSize(int)
    Parameter(매개변수):moveHorizontotal(50)->50이 매개변수.
    signature:메서드의 이름+ 자료형:
    changeColor+(string)=이렇게.
    type:int,string,boolean.
    state:객체가 가지고 있는 모든 attribue값의 현재 상태 snapshot.
    ****
    