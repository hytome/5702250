week2:class이해하기. class=object design blueprint.
Fields:object save data(state).
constructors(생성자)=객체를 초기화 하는 methods.
methods = 객체의 action정의. 동작 정의.
parameters(매개변수)=매서드에 전달하는 값.
statements(문장):코드 단위 실행 명령. printing coditional,assigmentstatements.
****
ticket maschine. 다룰 클래스 .
maschine sells ont type tiket. =단일 종류 티켓 판매.
one price. =고정된 가격
collects the correct amount of money = 지불된 금액 수집하는 기능.
****
이제 fixed price . determind = 어떻게 가격 결정.
how is money enterd=돈 어케 입력. track money= 어떻게 돈 추적?
****
외부 vs 내부 관점.
외부에서 객체와 상효적용하면. behaviour:외부에서 객체와 상호작용. loking inside: 기능 어떻게 구현되어있는지 확인하기.
all java classes have a similar-looking internal view.
객체 내부 구조 분석하고 이해하기.
public class TiketMashine{ ->the outer wrapper of ticketMashine.
    inner part of the class 
}
public class class name { ->the contents of a classe
Fields
constructors
Methods
}->보통의 일반화된 구조.
Fildes:Characterize an object,ie contain specific value:객체의 속성/데이터 필드.
Constructors:initialize objects = ㅑㅡㅡ객체 생성할때 초기값.
Methods:implement object behavior after initialization,ant kind of futher:객체의 기능/동작을 정리함.
****
Filde=store value for object = 객체 내부에서 값을 저장한다.
They are also known as instance variables. = 인스턴스 변수라고도 부른다.
예시.
public class TicketMachine{
    private int price;
    private int balance;
    private int total;
} 
private:visibility modifier 
int:type
price:valuable name.
Fields = define the state of an object.
****
comments:주석. important for code documention. adding 주석 is goodprogramming style 
one-line://
multi-line :/* */
javadoc: /** */
****
Constructors(생성자)
public TicketMachine(int ticketcost){
    생성자:they have the same name as their class= 클래스 이름과 동일 해야함.
    용도:they store initial valeus into the fileds:객체가 생성될때 필드 초기화.
    they often receive extranl parameter values for this:초기화할 값을 외부에서 받아온다. 매개변수
    int ticketcost=parameter.
    ticketcost=price.
    balance=0;
    total=0; ->잔액과 총수익은 0.으로 시작. price는 티켓코스트를 파라메터로 받아서 초기화.
}
****
Assignment(대입문.)
Values are stored into fields(and other variables) via assignment statements:변수나 필드에 값을 저장(대입 하는 구문)
-variable = expression;
 price=ticketCost;
 A variable stores a single value so any privious value is a lost. = 변수는 한번의 하나의 값만 저장가능, 이전 값은 덮어써지면서 사라짐.
 ****
 methods 메서드:gerPrice,getBalance,insertMoney,Printtiket.
 ****
 Accesoor Method. 접근자 메서드드
implement the behavior of objects. and accessor provid information . 읽기전용. 객체의 정보를반환함함
Method=header+body. heder defines signature.
body contain statement 
example:public int getPrrice()
getter 이라고도 불림.
****
계속..
public int getPrice(){
    return price
}
public:visibility modifier
int:return type.
getPrice:method name.
():parameter list(now empty.)
return price:return statement.
{}:start end method block.
****
Mutator Method.
Similar structure (header and body)
Used to change an object state = mutate 를 사용해서 객체의 상태를 변경할수있다. assignment statements 사용.
객체의 상태는 하나이상의 필드를 바꾸는 것으로 달성된다.
****
예시)
public void insertMoney(int amount){
    balance = balance+amount;
    balance=field being mutated.
    balance+ amount=assignment statement.
}
have no return type.
****
total = total+ balance; = update total collected with whe balance.
balance = 0; =clear balance.
insertmoney해결.
public void insertMoney(int amount){
    if(amount>0){
        balance= balance+amount; 0보다 크게 해결
    }else{
        system.out.println("use a postine amount :" +amount);
    }
}
****
local variables 지역변수.
methods can include shorter-lived variables.
exist oniy during method excution. accessible oniy within the method.
선언 위치 생존기간 접근범위 전부 메서드 실행중만 존재할수있다.
****
scope and life time.
scoe: