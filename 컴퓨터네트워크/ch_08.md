## ch_08
기말은 전체 범위.
network topology.
1.bus 커넥션 방식 이 방식은 연결이 쉽고,확장성이 용이하다. 
2.star ->가운데에 컴퓨터 하나. 주변에 있는 컴퓨터들이 별처럼 이어져서 네트워크를 받는다. 이더넷 스위치,라우터는 스타 토플로지를 갖고있다.
스타 토폴로지의 장점은 중앙에 연결된 방식이라. 네트워크에 있는 정보를 전부 열람할 수 있다. 거쳐가는 방식이기 때문이다. 이 노드를 통해서만 연결이 가능하다.
이것이 장점이다.
3.ring->둥글게 둥글게 옆에 있는 컴퓨터 하고만 통신을 할수 있는 구조다.
이 두가지 방식은 이더넷이고 
버스는 자동차나 비행기에 자주 쓰이는 구조이다.

in.output port에는 queue가 반드시 존재해야한다.
들어오고 나가는 패킷을 잃어버리지 않도록 큐로 버퍼를 구성한다.
그래서 보통 이걸 큐에 넣었다,라고 말한다. first in first out.
buffer management.
버퍼의 모든 패킷이 다차면 그이후로 들어오는건 drop 이되거나 패킷로스 발생.
어쩔때는 overwrite. 다찬 상태에서 늦게들어오는건 받지않게 설계도 가능하다. 
어쨋든 버퍼가 다찬 상태에서는 loss 발생. 이것을 drop이라고한다.
tail drop: 

priority:
패킷 스케쥴링:fcfs.
우선순위가 서로 다른 큐를 만들어 처리할떄는,우선순위를 결정해야한다.
우선순위에 따라 남은 패킷은 아예 처리가 안된다.
round robin:원판돌리기,한번씩 번갈아가면서 순서를 정하는 스케줄링.서로 처리할 확률을 정해놓고 가장 높은 우선순위에게 확률 가중치를 주는게 weighted fair queue다.
즉 가중치를 주고 높은 확률로 빨리 버퍼를 처리하게 우선순위를 주는것이다.
요거 계산하는거 시험문제에 나올꺼같음 공식보고 w1,2,3 값주고 우선순위 확률 계산하기.
****
네트워크 레이어.
ip의 주요담당은 주소. addressing. 주소->네트워크 레이어.
네트워크 레이어에서ip=소프트웨어,즉 vpn같은 걸로 바꿔진다는 의미로써의 개챵챵념.
물리적으로 통신사에서 주는 ip는 link에서 다룬다.
ip datagram format 외울 필요 없음.
ip adress=32 비트.한 공간에 8비트 숫자로 4개로 이루어져있음.
라우터의 여러개의 포트 또다른 네트워크가 연결되어있다. 서브네트워크,서브넷이라고 부른다. 서브넷끼리 통신을 할때는 라우터를 통과할 필요가 없다.
cidr-크게 안중요하다.
how to get one?->모바일이라 가정하지 않고 그주소는 바뀌지 않는다고 확신하기. 고정된 값을 하드코딩해서 쓰기 (예전에는 이렇게함)
동적으로 ip 주소 할당시키기->이게 DHCP plug-and-play- 코드를 꽃고 바로 사용한다.
나 주소 바꼈다고 서버와 클라이언트에게 통신을 하며 업데이트 하는것 dhcp.
터널링=기존의 ipv4를 ipv6로 서로 호환되게 하는것
tunneling